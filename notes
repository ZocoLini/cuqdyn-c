-- Original matlab code notes --

Here, we focus on dynamic models represented by deterministic nonlinear ordinary differential
equations
we propose the use of conformal inference methods, introducing two novel algorithms that

-- New C code notes --

- el código de cada carpeta corresponde a cada uno de los
   ejemplos, por lo que el código del metodo está repetido en cada
   una. Simplemente habría que generalizarlo para que admita un
   modelo en ODEs arbitrario. Respecto al metodo, centraros en
   CUQDyn1 (el 2 lo podéis ignorar). Yo empezaría por el problema
   Lotka-Volterra.

- para ODEs, normalmente usamos CVODE (o CVODES si queremos
  sentitivities para el gradiente). Parte de la libreria SUNDIALS y
  disponible en